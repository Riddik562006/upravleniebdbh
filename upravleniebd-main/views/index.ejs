<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Управление финансами</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            padding: 8px;
            border: 1px solid #ddd;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        .buttons {
            margin: 20px 0;
        }
        button {
            margin-right: 10px;
        }
    </style>
</head>
<body>
    <h1>Управление финансами</h1>
    
    <div class="buttons">
        <button onclick="window.location.href='/add-operation'">Добавить операцию</button>
        <button onclick="window.location.href='/add-portfolio'">Добавить портфель</button>
    </div>

    <h2>Финансовые операции</h2>
    <table>
        <thead>
            <tr>
                <th>Дата</th>
                <th>Сумма</th>
                <th>Категория</th>
                <th>Действия</th>
            </tr>
        </thead>
        <tbody>
            <% operations.forEach(operation => { %>
                <tr>
                    <td><%= new Date(operation.Date).toLocaleDateString() %></td>
                    <td><%= operation.Summa %></td>
                    <td><%= operation.Category %></td>
                    <td>
                        <button onclick="window.location.href='/edit-operation/<%= operation.FinOperationID %>'">Изменить</button>
                        <form action="/delete-operation/<%= operation.FinOperationID %>" method="POST" style="display:inline;">
                            <button type="submit">Удалить</button>
                        </form>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>

    <h2>Инвестиционные портфели</h2>
    <table>
        <thead>
            <tr>
                <th>Название</th>
                <th>Количество акций</th>
                <th>Стоимость</th>
                <th>Действия</th>
            </tr>
        </thead>
        <tbody>
            <% portfolios.forEach(portfolio => { %>
                <tr>
                    <td><%= portfolio.Nazvanie %></td>
                    <td><%= portfolio.KolichestvoAkciy %></td>
                    <td><%= portfolio.Stoimost %></td>
                    <td>
                        <button onclick="window.location.href='/edit-portfolio/<%= portfolio.InvestPortfolioID %>'">Изменить</button>
                        <form action="/delete-portfolio/<%= portfolio.InvestPortfolioID %>" method="POST" style="display:inline;">
                            <button type="submit">Удалить</button>
                        </form>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</body>
</html>